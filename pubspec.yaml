name: paa_projek
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  http: ^1.2.1
  flutter:
    sdk: flutter
  xml: ^6.5.0
  geolocator: ^11.0.0
  geocoding: ^3.0.0
  lottie: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  google_fonts: 6.2.1

flutter:
  uses-material-design: true
